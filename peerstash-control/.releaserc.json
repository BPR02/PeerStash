{
  "branches": [
    "main"
  ],
  "tagFormat": "peerstash-control-v${version}",
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "preset": "angular",
        "parserOpts": {
          "noteKeywords": [
            "BREAKING CHANGE",
            "BREAKING CHANGES",
            "BREAKING"
          ]
        },
        "releaseRules": [
          {
            "scope": "!*control*",
            "release": false
          }
        ]
      }
    ],
    [
      "@semantic-release/npm"
    ],
    [
      "@semantic-release/exec",
      {
        "publishCmd": "echo ${process.env.GITHUB_TOKEN} | docker login --username ${process.env.REPO_OWNER} --password-stdin ghcr.io && docker build -t ghcr.io/${process.env.REPO_OWNER}/peerstash-control:${nextRelease.version} . && docker push ghcr.io/${process.env.REPO_OWNER}/peerstash-control:${nextRelease.version} && docker build -t ghcr.io/${process.env.REPO_OWNER}/peerstash-control:latest . && docker push ghcr.io/${process.env.REPO_OWNER}/peerstash-control:latest"
      }
    ]
  ]
}
